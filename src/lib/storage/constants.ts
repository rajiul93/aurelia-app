/** Minimum free device space required before starting a tour download. */
export const MIN_FREE_SPACE_BYTES = 250 * 1024 * 1024;

/** Warn in Settings when free space drops below this threshold. */
export const LOW_STORAGE_WARNING_BYTES = 500 * 1024 * 1024;
